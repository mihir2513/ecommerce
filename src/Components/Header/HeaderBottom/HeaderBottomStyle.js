import { makeStyles } from "@material-ui/core";

const HeaderBottomStyle = makeStyles(() => ({
  headerBottom: {
    backgroundColor: "transparent",
    padding: "25px 0",
    borderBottom: "1px solid #eeeeee",
    display:'flex',
    margin:"auto",
    justifyContent:"center",
  },
}));
export default HeaderBottomStyle;
